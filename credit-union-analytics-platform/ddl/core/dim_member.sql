create or replace TABLE CU_DB.CORE.DIM_MEMBER (
	MEMBER_ID NUMBER(38,0),
	JOIN_DATE DATE,
	DOB DATE,
	STATE VARCHAR(16777216),
	RISK_SCORE NUMBER(38,0),
	KYC_STATUS VARCHAR(16777216),
	PEP_FLAG BOOLEAN
);